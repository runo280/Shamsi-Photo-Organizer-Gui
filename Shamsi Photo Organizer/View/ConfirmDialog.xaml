<Window x:Class="Shamsi_Photo_Organizer.View.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        Title="تاییدیه">
    <Grid Margin="15" FlowDirection="RightToLeft">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Name="LblTotal" Grid.Column="0" />
        <Label Grid.Row="1" Name="LblValid" Grid.Column="0" />

        <Button Grid.Row="2" Grid.Column="0" Click="btnDialogOk_Click" MinWidth="60" Margin="0,0,10,0">تایید</Button>
        <Button Grid.Row="2" Grid.Column="1" IsDefault="True" IsCancel="True" MinWidth="60">انصراف</Button>

    </Grid>
</Window>